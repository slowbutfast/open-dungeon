# Handoff — narrator-style-fidelity (2026-08-05)

This handoff captures the state after implementing, verifying, and archiving the
`narrator-style-fidelity` change. Read it alongside
`openspec/changes/archive/2026-08-05-narrator-style-fidelity/` (proposal,
architecture, specs, tasks, tests, research) and
`docs/handoffs/2026-08-05-spatial-map-handoff.md` (prior context).

## Repository state

- Branch `master`. Three new commits ahead of the previous handoff tip:
  - `41d7b13` — status mandate + style directive + `[NARRATOR STYLE]` block (the core change)
  - `e155964` — simple-action budget floor (`SIMPLE_ACTION_MIN_TOKENS=200`) + truncated-`[Status:` fragment stripping
  - `8925826` — deterministic stale-status recovery (GH #38) via `engine/narrationLandmarks.js`
  - plus the archive commit (change dir moved to `openspec/changes/archive/`).
- Working tree clean except the `tests/e2e/screenshots/*.png` regenerated by the e2e runs
  (owned by the in-progress `fix-mobile-view-responsiveness` WIP — do not commit them with
  unrelated work).

## What was implemented (all committed)

1. **Prompt contract:** `DEFAULT_SYSTEM_PROMPT` + 4 presets + `web/static/js/app.js` now mandate
   "the Location field MUST name the new place when the player moves" and the style directive
   ("Adopt the tone implied by the player's opening and hold it consistently"). Pinned by
   source-text tests in `tests/test_engine_status_parsing.py`.
2. **Pinned narrator style:** `engine/narratorStyle.js` (deterministic keyword classifier,
   default `direct`); `state.narratorStyle` persisted (`narrator_style`, additive);
   `[NARRATOR STYLE]` registry block in `engine/contextBlocks.js` (sanitizer auto-covered);
   `AdventureEngine.setNarratorStyle` explicit override.
3. **Output-budget floor:** `computeNarrationBudget` (`engine/llm.js`) — simple actions floored
   at 200 tokens so the mandated status line can't be truncated off; movement verbs uncapped.
4. **Truncated-fragment hygiene:** `sanitizeForHistory` strips `[Status:` lines without a `]`.
5. **Stale-status recovery (GH #38):** `engine/narrationLandmarks.js` (pure extractor) + turn-commit
   hook in `engine/llm.js`. Fires when the status line is MISSING or the narrator REPEATS its own
   previous status line (`llm.lastStatusLocation`, session-scoped, reset on `newAdventure`/`load`
   because the MCP server reuses one engine). A CHANGED status always wins; non-arrival prose
   recovers nothing.

## Test status

- `npm run test:unit`: **126 pass, 3 fail** — the 3 failures are pre-existing intentional TDD
  placeholders (`migration.test.mjs` "RED on HEAD", two `structuredStore.test.mjs`
  "INTENDED TO FAIL TODAY" trade-undo cases) owned by the in-progress `make-undo-and-trades-consistent`.
- `venv/bin/python -m pytest` broad suite: **362 pass, 3 fail** — same pre-existing trade-undo
  limbo tests (`test_barter_engine.py`, `test_undo_consistency.py`). No regressions.
- New tests: `tests/unit/narratorStyle.test.mjs`, `tests/unit/narrationBudget.test.mjs`,
  `tests/unit/narrationLandmarks.test.mjs`, mock stale-status integration in
  `tests/unit/spatialIntegration.test.mjs`, source-text pins in `tests/test_engine_status_parsing.py`.

## Acceptance gate

**PASSED** (2026-08-05): one natural 12-turn live Wanderer session on the DEFAULT model
(`cognitivecomputations/dolphin-mistral-24b-venice-edition`) grew the map **1 → 9 rooms**.
Notably, the mandate alone did NOT move the default model — it emitted a usable status line on
only 1/12 turns. The gate passed **entirely via the D6 stale-status recovery backstop**
(10 recoverable turns). `narrator_style` pinned `grim` and constant. Cost ≈ $0.009. Data in
`game/playtest/results-accept-gate-live.json` (probe `probe-accept-gate-live`).

## New findings from the gate playtest

- **Major (pre-existing, follow-up):** `engine/memory/roomMap.js:98` `WALK_VERBS = /^(go|walk|…)\b/i`
  is anchored at string start, so natural first-person actions (`"I follow a deer path…"`) classify
  as `unknown` → **no walk edges** in natural play (rooms grew to 9, edges stayed at 1). Fix: allow an
  optional leading `I ` / `I've` before movement verbs. Orthogonal to the gate (rooms > 3 passed).
- **Minor (recovery quality):** `narrationLandmarks.js` produces occasional false-positive/truncated
  room names from prose (`Spine` from "a chill runs down your spine", `Left`, `Cluster of Low`,
  `Shallow`). Cosmetic — no fabricated connectivity. Optional hardening: anatomical/conjunct guard
  words.
- **Model reality check:** the mandate provably works on cooperating models (deepseek-v4-pro advanced
  its status line 3/3 on fully-emitted movement turns) but the default model's compliance is ~zero.
  "Narrator fidelity" claims should be worded as **recovery-guaranteed**, not compliance-guaranteed.

## Natural human playtest (follow-up session, 2026-08-05)

Played 18 turns on `google/gemini-2.5-flash` as a whimsical wanderer ("The Clockwork Meadow":
wake on a mossy hill → bronze trees → found a ticking clockwork device → placed it in a pedestal
niche → descended into a rune-lit subterranean chamber). Genuinely fun, coherent fiction; the
spatial map tracked the journey (8 rooms). The session surfaced **two real defects, now fixed**:

1. **Style detector mis-pin (fixed):** `narrator_style` pinned `terse` because detection scanned the
   system prompt, whose "concise and curt" mandate matched the terse keyword list and overrode the
   player's whimsical opening. Fix: detection signals are now title + the player's opening words only
   (`engine/llm.js`, `detectNarratorStyle(state.title, text)`); added `curious`/`delightful`/etc. to
   the whimsical keywords. Regression test: `spatialIntegration.test.mjs` "whimsical opening is NOT
   mis-pinned terse".
2. **Recovery noise on repositioning prose (fixed):** with models that emit no status lines at all
   (gemini did not), every turn hits the missing-status recovery, and everyday action prose became
   spurious rooms (`Wall` from "step up to the wall", `Pace` from "step back a pace", `Ticking` from
   "following the ticking", plus a muddled `Clockwork Meadow the Path`). Fixes in
   `engine/narrationLandmarks.js`: `step` now requires a destination preposition (`step into`, not
   `step up to`), `follow` requires a route noun (`follow the path`, not `following the ticking`),
   and manner adverbs (`slowly`, `carefully`, ...) are stopwords. Regression tests in
   `narrationLandmarks.test.mjs`.

Session also reconfirmed GH #37 (injected "Eldrin, a Mage" persona collided with my opening) and
GH #39 (0 edges from first-person actions). Data: `game/playtest/results-natural-human-play.json`,
probe `probe-human-play` (server stopped).

## Open items / suggested next steps

1. **`make-undo-and-trades-consistent` (23/24)** — still in progress; owns the 3 pre-existing
   failing unit tests + 3 python tests. Finish and review before treating as done.
2. **`fix-mobile-view-responsiveness` (39/40)** — the e2e screenshot diffs in the working tree
   belong to it.
3. **`playtest-diagnostics-hygiene` (0/16)** — per-adventure `llmTracker` scoping.
4. **GH issues:** #35 (map viz + BFS), #36 (narrator status-line fidelity), #37 (persona override),
   #38 (stale-status recovery — **implemented, closed**), **#39 (new)** — WALK_VERBS `^` anchor for
   first-person actions (natural-play edge loss).
5. The in-progress `playtest-diagnostics-hygiene` change and the WALK_VERBS anchor are the highest
   value next work after the undo/trades WIP is closed.

## Conventions / guardrails (unchanged)

- `playtest` subagents drive isolated probe servers over HTTP via `tests/probe_runner.py`; never the
  shared registered `dungeon_*` MCP tools in parallel.
- MOCK_LLM=1 for routine loops; real model only when needed, cost flagged.
- Never write to `game/adventures/` (production); use `game/playtest/`.
