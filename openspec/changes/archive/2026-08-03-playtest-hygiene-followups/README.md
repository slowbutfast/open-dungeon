# playtest-hygiene-followups

Retrospective: post-batch hygiene patch — sanitize state.summary, fix MCP env wiring (per-server environment + startup guard), and add the project-level playtest subagent.
