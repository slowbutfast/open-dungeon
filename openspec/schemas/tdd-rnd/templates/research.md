## Source material

<!-- Verbatim notes from meetings, the PI, collaborators, stakeholders. Dated and
     attributed. Quote in full rather than paraphrasing, so a later reader can audit
     whether the requirements derived from this were a fair reading. -->

### Raised but not acted on

<!-- What appeared in the source material that this change deliberately does NOT address,
     and why. Often more useful than the parts that were acted on — it stops the next
     person re-proposing something already considered. -->

## Glossary

<!-- Domain terms, acronyms, and any word this change uses in a non-obvious sense.
     The "Does NOT mean" column is the point: a definition alone does not prevent a
     wrong inference; naming the plausible misreading does. Add an entry the moment a
     term could be read two ways, not after someone gets it wrong. -->

| Term | Means | Does NOT mean |
| :--- | :--- | :--- |
|  |  |  |

## External research

<!-- Reference implementations, papers, standards, taxonomies, prior art outside this
     project. Record the licence wherever code is involved: "study only" vs "reusable
     with attribution" is a decision, not a footnote. Include date accessed — URLs rot.
     Record failed lookups too, so nobody re-runs a dead end. -->

| Source | What it establishes | Licence | Accessed |
| :--- | :--- | :--- | :--- |
|  |  |  |  |

## Candidate tech

<!-- Packages, frameworks, stacks, services considered — adopted AND rejected, each with
     a reason and a date. A rejected option with a date is useful twice: it stops
     re-evaluation, and it lets you notice when the reason has expired. -->

| Option | Decision | Reason | Date |
| :--- | :--- | :--- | :--- |
|  |  |  |  |

## Patterns adopted

<!-- Specific techniques taken from external sources: what was adopted, from where, and
     where it lands in this codebase. Patterns are not copyrightable; source code is —
     if a source is unlicensed, adopt the approach, never the lines. -->

## Verified facts

<!-- Claims this change depends on, each with the method that settled it. "How verified"
     is the load-bearing column: "the field exists" and "the values are usable" are
     different claims, and conflating them is the most common way a plan goes wrong.
     Volatility says what needs re-checking later. -->

| Claim | Value | How verified | Date | Volatility |
| :--- | :--- | :--- | :--- | :--- |
|  |  |  |  | stable / decays |

## Unverified assumptions

<!-- Believed but not checked, and what checking would cost. An assumption listed here is
     honest; an assumption recorded above as a verified fact is a future defect. -->

## Superseded claims

<!-- What was believed, why it was wrong, and what replaced it. Keep these rather than
     editing them away — a corrected claim is evidence about how the data or system
     actually behaves, and it stops the original belief being rediscovered. -->

## Links out

<!-- Project-scoped material this change rests on. Links, not copies — anything spanning
     multiple changes belongs in the project's docs, referenced from here. -->
